# --- Dependencies & Builds (Physical Layer) ---
node_modules/
dist/
dist-ssr/
# Lockfiles are machine-generated; manual formatting is System Noise
package-lock.json
yarn.lock
pnpm-lock.yaml

# --- Playwright (Automated Verification) ---
test-results/
playwright-report/
blob-report/
playwright/.cache/
# Visual snapshots are binary-like; Prettier will mangle them
**/*.spec.ts-snapshots/
**/*.spec.js-snapshots/

# --- Machine Generated (High-Integrity Caution) ---
# We ignore these to prevent Git Diff noise when re-generating
src/types/supabase.ts

# --- System & Env ---
.env
.env.*
*.log
.DS_Store
.eslintcache

# --- Documentation ---
# We let Prettier handle READMEs, but ignore local changelogs if auto-gen
# CHANGELOG.md

# --- ☢️ FORT DE SOTO: THE QUARANTINE ZONE ☢️ ---
# The Feral Agent's raw workbench. No aesthetic formatting allowed.
src/tests/quarantine/**/*.ts
