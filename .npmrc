# 1. Default Registry: Always use the public NPM registry (The Source of Truth)
registry=https://registry.npmjs.org/

# 2. Scope Lock: Explicitly tell NPM where to find YOUR private gems
@jameshood118:registry=https://registry.npmjs.org/

# 3. The Key: Use the token from your environment (System Variable)
# This allows access without hardcoding secrets.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
